// Drizzle schema. After wiring better-auth, run its CLI from apps/api to
// generate the user/session/account/verification tables into this file:
//   npx @better-auth/cli@latest generate
// Feature tables (trip, day, place, expense, member, etc.) will be added
// once the design is finalized.
export {};
